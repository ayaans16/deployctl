# include the vps details for deployment

# vps information
VPS_IP=
VPS_USER=

# use VPS_PASSWORD for password auth, or VPS_SSH_KEY_PATH for key-based auth
# (required for AWS EC2, which is key-only by default), set one, not both
VPS_PASSWORD=
VPS_SSH_KEY_PATH=